#include "chip.h"

int main() {
    //Chip_GPIO_Init();

    // make PIO0_7 an output
    //Chip_GPIO_SetDir(0, 7, 1);
    // turn it on
    //Chip_GPIO_SetValue(0, 7);

    // loop a bunch
    for (;;);

    return 0;
}
